# Implementation Plans

Generated by the improve skill (architecture-focused audit) on 2026-07-12,
against commit `a19e878`. Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

Audit scope: module boundaries, data-access layering, route structure,
component organization, caching/revalidation strategy, config/content
management — plus the security findings that surfaced along the way. NOT
audited: test coverage beyond "none exists", browser/a11y behavior, the docs/
planning specs, deployment/Vercel config. Run non-interactively, so plan
selection defaults to the top findings by leverage.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fix league cache invalidation gaps and cache-key collisions | P1 | S | — | TODO |
| 002  | Centralize league data access into lib/league-queries.ts    | P1 | M | 001 | TODO |
| 003  | Close the /admin proxy bypass; handler-level auth            | P1 | M | — | TODO |
| 004  | Pickup payments server-side; stop shipping credentials       | P1 | M | — | TODO |
| 005  | Consolidate infra duplicates (auth/db/env/config types)      | P2 | M | 003 | TODO |
| 006  | Remove dead code; fix broken lint toolchain                  | P2 | S | 004 (Step 4 only) | TODO |
| 007  | Rewrite CLAUDE.md orientation; fix session.ts comments       | P2 | S | 001, 002 (soft) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- 002 assumes 001's key renames, `revalidate: 300` backstop, and
  `lib/league-cache.ts` exist.
- 005 builds on the `getAdminSession()` and secret guards 003 adds to
  `lib/auth.ts` / `lib/league-auth.ts`.
- 006 Step 4 (delete `getPaymentStatus`) only applies after 004 confirms the
  payments page no longer could use it; all other 006 steps are independent.
- 007 documents the caching model 001/002 establish; if executed first, it must
  document the pre-001 reality instead (its drift check covers this).
- 003 and 004 are independent of everything and safe to run first if security
  is the priority.

## Operator actions no plan can do

- **Rotate the burned credentials** after 004 lands: the Google Sheets API key
  and the pickup admin password both shipped in public JS bundles
  (`NEXT_PUBLIC_SHEETS_API_KEY`, `NEXT_PUBLIC_ADMIN_PASSWORD`). Treat both as
  compromised regardless of code changes.
- Decide the true pickup session time: `lib/calendar.ts` says 5:30 PM,
  `lib/session.ts` code says 8:00 PM (see plan 007 STOP conditions).

## Findings considered and rejected

- **Dual chart libraries (chart.js + recharts)**: real duplication split along
  the merge seam, but consolidation is L-effort UI rewriting for a modest
  bundle win — accepted debt, recorded in plan 006.
- **`revalidateTag('league', 'max')` second argument**: possibly inert on this
  Next version, but harmless and consistent; folded into plan 001's helper
  rather than flagged separately.
- **Standings SQL over-computation** (`getTeamRecord` scans all seasons —
  `app/league/team/[teamId]/page.tsx:75`; standings page triple-scans
  `fixtures⋈match_results`): MED confidence on real-world cost, masked by the
  cache; deferred to a follow-up with EXPLAIN evidence (noted in plan 002's
  maintenance notes).
- **Build-timeout risk from prerendering DB pages**: cleared — no
  `generateStaticParams` anywhere; all DB pages are `revalidate = 0` or
  `force-dynamic`. Documented as a "don't regress" note in plan 007.
- **God-file refactors** (`FixturesCalendar.tsx` 778 lines,
  `app/league/admin/fixtures/page.tsx` 724, `TeamDrafter.tsx` 718,
  `lib/league-fixtureUtils.tsx` 599): genuine, but refactoring them without
  any test baseline is higher risk than reward right now; revisit after a test
  harness exists.
- **League visual-silo consolidation** (own Avatar, `league.css`, no shared
  tokens): deliberate-looking seam from the merge; consolidating is L-effort
  with visual-regression risk and no functional payoff; the boundary is
  otherwise clean (no cross-imports found).
- **`(site)` admin CMS routes lack `revalidatePath`**: not a bug — all
  `(site)` public pages are `revalidate = 0` (uncached), so invalidation is
  moot there.
